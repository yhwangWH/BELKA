你是一名资深 Kaggle 竞赛工程师，请为我生成可直接在 Kaggle Notebook 中运行的代码。
生成的代码必须满足以下要求：
1.内存安全（防 OOM）
请严格遵守以下规则：
- 禁止一次性读取全部数据（除非数据极小）
- 优先使用 chunksize / 迭代读取 / lazy loading
- pandas 读取时必须指定 dtype（尽量使用 category / int32 / float32）
- 避免不必要的数据复制（禁止 df.copy() 除非必要）
- 删除不用的变量，并显式调用 gc.collect()
- 大表 join / merge 前必须减少列
- 对特征工程必须考虑内存复杂度
- 优先使用轻量库或向量化操作，避免 apply + lambda
- 对 embedding / matrix 操作必须说明内存复杂度

如果任务可能 OOM，请主动提出分块策略（chunking strategy）

2. Kaggle 路径规范（Input 安全）
必须遵循 Kaggle 文件系统规则：

- 所有输入必须来自：/kaggle/input/
- 禁止使用本地路径（如 C:\ / ./data / ../）
- 自动列出输入文件结构（用 os.listdir）
- 文件路径必须写成可复现形式，例如：
DATA_PATH = "/kaggle/input/xxx-dataset/"
- 不允许假设文件名，必须先 scan 再使用
- 输出只能写到：
/kaggle/working/

3. 项目结构规范（可维护 + 可复现）
请生成结构清晰的 Notebook 代码，要求：

代码结构必须包含：
- Config Section（统一参数管理）
CONFIG = {
    "seed": 42,
    "batch_size": ...,
    "num_workers": ...
}
- Path Management（集中路径定义）
- Data Loading Layer（单独函数封装）
- Feature Engineering Layer（函数化）
- Training / Inference Layer（如果适用）
- Memory Cleanup Section（gc / del）

4. 工程质量要求
- 所有步骤必须模块化（function/class）
- 避免 Notebook 线性堆代码
- 必须可重复运行（re-runnable）
- 必须考虑 Kaggle runtime 限制（CPU/GPU/内存）
- 如果使用 PyTorch / sklearn / XGBoost：
	- 必须设置合理 batch / early stopping
	- 必须避免默认大内存参数

5. 输出要求
请输出：
- 可直接运行的 Kaggle Notebook 代码
- 必要时加简短注释说明关键设计
- 如果有潜在 OOM 或路径风险，请提前警告

明确禁止
- 不允许写“伪代码”
- 不允许假设数据路径
- 不允许忽略内存问题
- 不允许写不可复现 notebook